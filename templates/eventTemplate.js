const { Client } = require("discord.js");

// Must mention client
// After the client parameter you can mention all the other parameters for that event
module.exports = ( /**@type {Client} */ client, some, xyz, aaa) => {
    // Code goes here
}